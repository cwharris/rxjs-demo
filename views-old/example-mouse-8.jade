extends layout

block styles

	style

block content

	h1 Example 2

block scripts

	script

		var canvas = $('canvas').get(0);
		var ctx = canvas.getContext('2d');

		$(document).onAsObservable('resize').startWith().subscribe(function() {
			canvas.width = window.width;
			canvas.height = window.height;
		});