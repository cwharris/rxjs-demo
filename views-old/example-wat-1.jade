extends layout

block scripts

	script

		// Functional Code!

		var nums = [1, 2, 3, 4, 5, 6, 7, 8];

		var squares = nums.map(function(n) {
				return n * n;
			});


		console.log(nums);
		console.log(squares);